name: Jarvis Assistant

on:
  workflow_dispatch:
  repository_dispatch:
    types: [chatgpt_task]

jobs:
  execute-command:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Git
        run: |
          git config --global user.name "Jarvis Assistant"
          git config --global user.email "bot@jarvis.ai"

      - name: Save Command
        run: |
          echo "${{ github.event.client_payload.command }}" > command.txt
          echo "Команда від Джарвіса збережена."

      - name: Execute Command
        env:
          GITHUB_TOKEN: ${{ secrets.GPT_TOKEN }}
        run: |
          COMMAND=$(cat command.txt | cut -d' ' -f1)
          FILE=$(cat command.txt | cut -d' ' -f2)
          CONTENT=$(cat command.txt | cut -d' ' -f3-)

          if [ "$COMMAND" == "create_file" ]; then
            echo "$CONTENT" > "$FILE"
            echo "🤖 Файл $FILE створено. Чекаємо підтвердження для пушу."
            if [ "${{ github.event.client_payload.confirm }}" == "yes" ]; then
              git add "$FILE"
              git commit -m "🤖 Автоматичне створення $FILE від Jarvis"
              git push origin main
              echo "✅ Файл $FILE успішно відправлено у репозиторій."
            fi
          fi
